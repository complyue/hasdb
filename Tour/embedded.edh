
import * 'db/demo/embedded'

runDbApp( '/var/tmp/demo', DbApp() )

dir

jims


{
  debug&>{
    # destruct something with something left, for each run
    # to change some thing
    compl.delete()
    dev.delete()
  }

  {#
   # this is similar to Pythonic idiom:
  >>> try:
  >>>   jim.version += 1
  >>> except TypeError:
  >>>   jim.version = 1
   # but a little less specific about the exception type
   #}
  jim.version += 1 $=> jim.version = 1
}


shutdown

quit
